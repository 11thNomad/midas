# ============================================================
# NiftyQuant Configuration
# Copy this to .env and fill in your actual values
# NEVER commit .env to version control
# ============================================================

# === Zerodha Kite Connect (Execution) ===
KITE_API_KEY=your_kite_api_key
KITE_API_SECRET=your_kite_api_secret
# Must match the redirect URL configured in Kite app settings.
KITE_REDIRECT_URL=http://127.0.0.1:8000/kite/callback
# Access token refreshes daily — handled by auth flow in broker.py
KITE_ACCESS_TOKEN=

# === TrueData (Optional secondary feed) ===
TRUEDATA_USERNAME=
TRUEDATA_PASSWORD=

# === Telegram Alerts ===
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
TELEGRAM_CHAT_ID=your_chat_id

# === Environment ===
# Options: development | paper | live
TRADING_MODE=development

# === Database ===
# SQLite for local dev, Postgres URL for VPS deployment
DATABASE_URL=sqlite:///data/trades.db
